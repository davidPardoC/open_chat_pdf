import { Button, Container } from "@chakra-ui/react";

const Library = () => {
  return (
    <Container maxW={"container.lg"} padding={1}>
      <Button>Agregar Documento</Button>
    </Container>
  );
};

export default Library;
